type Query {
  bookById(id: ID): Book
  allAuthors: [Author]
  getAllBooks: [Book]
}

type Book {
  id: ID
  name: String
  pageCount: Int
  author: Author
}

type Author {
  id: ID
  firstName: String
  lastName: String
  ssn: String
}
